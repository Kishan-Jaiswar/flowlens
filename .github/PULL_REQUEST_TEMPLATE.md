## What this changes

<!-- One or two sentences. -->

## Why

<!-- The problem, not the patch. If it fixes an issue, link it. -->

## Checklist

- [ ] `npm run verify` passes (lint, format, build, tests)
- [ ] Added a test that would have failed before this change
- [ ] If a heuristic was added, there is also a test for the case where it
      should **decline** to answer — a wrong edge is worse than a missing one
- [ ] Updated `docs/ARCHITECTURE.md` if the reasoning behind a pass changed
- [ ] Updated `CHANGELOG.md` under `Unreleased`
